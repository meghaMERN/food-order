export default function CartItem({name,quantity,price}) {
    return <li className="cart-item">
        <p>{name} - {}</p>
        <p>
            <button></button>
            <span></span>
            <button></button>
        </p>
    </li>
}